# 第 7 周周会材料

> 本周核心问题：**实验 A 的三个前置门槛（M1、Task 1.3、排队论）是否都过了？**
> 日期：2026-09-18
> 详细记录：`分析.md`（M1 复跑 / guardTime 根因 / 重标定 / ExpA-Pilot）、
> `分析-task1.3与排队论.md`（Task 1.3 与排队论检索）

## 一页结论

**计划内的三项：M1 过了；Task 1.3 做了但发现它交付不了原定验收；排队论检索做了，且把第 6 周的一条关键判断证伪了。**

**计划外但必须做的一项：查出一个让 ExpA 全线跑不起来的缺陷**——09-15 的 guardTime 改动只覆盖了 `coreSwitch`，边缘发送端没跟上，导致 `ExpA-Pilot` / `ExpA-NoFDL` / `FDL-Calibrate` 全部在 t=1.94 ms 崩溃、标量全零。已修复并重标定。

| 门槛 | 结果 |
|---|---|
| M1（回环确定性用例） | ✅ 过。两次 OXC 预约间隔**恰为 τ=20 µs**，BCP offset 传导差**恰为 τ**；`TooShort ≡ Off` 1701 条全同；对 pre-FDL 基线非 `my ID` 差异 **0** |
| Task 1.3（实验 B 基线） | ⚠️ 跑完 44 run，但**第一阶段负载只有 0.24%，丢包恒为 0**，交付不了"带丢包的 Static/Dynamic 基线"。已补一组有负载的四模式基线顶上 |
| 排队论检索 | ✅ 10 条入 `refs.bib`；**但均为题录级，未取得全文**；并证伪了"排队论都假定多波长" |

> 当前结论：FDL 已解决"核心节点光域暂存"的机制问题，且 M1 已把 Scenario B 的 +τ 时序锁死；距离严格全光交换仍缺**输入 FDL 时序**与**控制面闭合**证据。ExpA 的阻塞项已全部清空，可以开跑。

## 本周完成

1. **M1 全量复跑通过**，并顺带跑完此前未跑的 `Compatibility` / `SO` 与 3 个负向用例。
2. **修两个审核遗留缺陷**：`Test-FDL-TauMismatch` 因 NED 绑定而永不可能失败（换成两个真正可达的负向用例）；负向用例以 `0xC0000005` 崩溃（gdb 追出三个独立根因，含 `lambdasPerPort` 堆越界写与 9 个模块析构释放未初始化指针）。
3. **查出并修复 ExpA 全线崩溃**（guardTime 只改了一半），重标定后 **ρ=0.40 → 35.6 µs**，平台检查全绿。
4. **Task 1.3**：四模式重跑 68 个 run，产出对照图；补有负载的四模式基线。
5. **排队论检索**：10 条入 `refs.bib`，更正 3 处归属/口径。

## 本周证据

**M1 时序（决定性）**

```
Received BCP: inPort=3, burstSize=1437B, offset=0.000998744
OXC Reserved (FDL): 1st 3->4 (conn=0.10600412784); 2nd 4->2 (conn=0.10602412784)
→ 两次预约间隔恰 20 µs = τ；sat3 收到的 BCP offset 差恰为 τ
```

**四模式基线（`useFDL=false`，标定负载）**

| 模式 | ρ≈0.59 | ρ≈0.40 | 平均突发长 |
|---|---|---|---|
| **Static (3)** | **17.18%** | **11.77%** | 4295 B |
| NoPreemption (1) | 18.49% | 12.79% | 4295 B |
| Dynamic (0) | 18.71% | 12.87% | 4210 B |
| RoundRobin (2) | 18.97% | 13.28% | 1613 B |

第一阶段 44 个 run：`ΣburstLossTotal ≡ 0` —— **平台远离竞争区**，这是 Task 1.3 交付不了原定验收的直接原因。

图：`Examples/RingFdlOBS/results/task1.3-2026-09-18/phase1_four_modes.png`

**排队论**：`refs.bib` 新增 10 条（Callegati 2000、Chlamtac 1996 CORD、Hunter 1998 SLOB、Nizam 1998 WASPNET、**Laevens & Bruneel 2003 单波长**、**Van Houdt 2004 单波长 FDL 利用率与丢包率**、Rogiest 2007/2009、Lambert 2008 FDL 长度优化、Lambert 2006）。

## 本周判断

**支持**：M1 已过，Scenario B +τ 时序可信；ExpA 可以开跑；Static 在无 FDL 时优于 Dynamic 约 1.1 pp，给实验 B 一个明确先验；四种模式的 P1–P4 命中构成与聚合行为已被量化。

**不支持**：把第一阶段配置当作实验 B 基线（负载 0.24%，零丢包）；把排队论当作"已读"（仅题录级）；再说"排队论都假定多波长/全波长变换"（已被 `laevens2003single` / `vanhoudt2004channel` 证伪）；把第 5 周标定当回环性能。

**需要正视**：排队论一线补齐后，本文与已有解析工作的差别**只剩三条**——载荷器件代价、星间场景、与无缓存重传的量化对照。`vanhoudt2004channel` 的指标口径与 5.1 直接重叠，`lambert2008hessenberg` 已做 τ 优化。

## 下周门槛（第 8 周）

| 顺序 | 事项 | 说明 |
|---|---|---|
| 立即 | 取得 `laevens2003single` 与 `vanhoudt2004channel` 全文/摘要 | 否则 2.1 只能写题录级陈述 |
| 第 8 周计划 | 热点流量矩阵 `FDL-Hotspot`，确认瓶颈 ρ≥0.8 | 全互联最高只到 0.59；ρ=0.8 必须靠热点实测 |
| 第 8 周计划 | 确认 D1 询问稿已发出 | 冻结日 2026-09-28；逾期用 Mouammar 开关表默认值 |
| 可并行 | ExpA 批量（`ExpA-TauSweep`，5 τ × 2 负载 × repeat 5 = 50 run） | 阻塞项已清；注意 50 run 是诊断基线之外的正式实验 |
| 不要做 | 实验 B 正式跑之前的 `RoundRobin` 对照 | 其突发长与其余三种差 2.7 倍，公平性未解决前不进 2×2 |
