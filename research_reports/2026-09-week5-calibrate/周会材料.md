# 第 5 周周会材料：实验平台标定

> 日期：2026-09-04；2026-09-09 修正 `stopTime`
> 本周核心问题：发送间隔设多少才对应目标负载？W=1、多源、随机化有没有生效？

## 一页结论

配置和脚本已经按第 5 周门槛改完。W=1 从只改 sat1/sat2 改成 13 条 ISL + 10 个节点全部冻结；标定扫描改为 7 档间隔 × 2 个种子。

**14 个 run 已由命令行一次跑完。** ρ=0.4 对应间隔 35.9 µs，已写回 `fdl_params.ini`。原估算 42.6 µs 只到 ρ≈0.35。本扫描最重点 ρ≈0.59，ρ=0.8 达不到。

> 当前结论：回环 FDL 的功能路径上周已经收口；本周要解决的是“实验到底跑在多大负载上”。距离严格全光交换仍缺少标定后的性能区间。

## 本周完成

1. 把 `FDL-Scenario` 的波长参数全部写死为 1，避免再吃 `params.ini` 里 sat1—sat2 的 3 波长。
2. `finish()` 增加 `portLambdas[p]`，让 W=1 能从 `.sca` 直接核对。
3. `FDL-Calibrate` 加上 `repeat=2`；`calibrate_load.py` 同时拟合 k 并检查多源与种子差异。

未改 `src/EdgeNode/`，未改调度判断。新增标量后需要重新 `make MODE=release`。2026-09-09 把 `stopTime` 从 `0s` 改为 `-1s`，否则 `UDPBasicApp` 在初始化时直接报 `Invalid startTime/stopTime parameters`；这一处只改 ini。

## 本周证据

静态 W=1（`params.ini` 保持不动，FDL 场景覆盖）：

| 对象 | 第一阶段 General | FDL-Scenario |
|---|---|---|
| sat1—sat2 数据波长 | 3 | 1 |
| 其余 ISL | 1 | 1（显式冻结） |
| sat1 / sat2 端口串 | `"1 3 1"` / `"1 3 1 1"` | 全 1 |

运行时证据：14 个 `.sca` 已齐。ρ=0.4 → 35.9 µs。W=1 / 多源 / 双种子均通过。明细见同目录 `分析.md`。

## 用户待跑

第 5 周标定仿真已在命令行完成。下一步是第 6 周文献定位；实验 A 不要在第 6 周之前开跑。

## 下周门槛（第 6 周）

第 6 周是文献定位（go/no-go）：FDL/OBS 竞争缓解已有工作梳理，并写出“本文新在哪”。本周 k 未回写时，不要开始实验 A。
