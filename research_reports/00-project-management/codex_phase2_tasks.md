# OBSmodules 第二阶段及后续研究执行计划

> 项目名称：卫星星间全光交换仿真
> 技术栈：OMNeT++ 4.x / C++ / NED
> 研究投入：每个研究周 2 个完整工作日
> 计划基线：16 个研究周完成 FDL 验证、第一篇论文实验与投稿；实验 C 在第 17 周起
> 更新日期：2026-08-14；**2026-09-14 再修订（论文骨架落地，第 7–16 周重排）**
> 本次修订：修正 FDL 架构定位；补入实验平台重建、文献定位、Task 1.3 和论文并行线；修正 FDL 器件口径、BCP 假设标注、RateCheck 闸门判据、第 6/13 周强化、实验 C 前置任务
> 2026-09-14 修订：叙事改为载荷约束下回环 FDL vs 无缓存重传；实验 C 移出 16 周主线；补 VF 对照、重传基线、热点负载；D1 默认冻结日保持 2026-09-28。叙事唯一源：`research_status.md` 第十节。骨架：`research_reports/2026-09-week6-novelty/论文骨架.md`。

---

## 一、计划定位

本文件是当前确认的执行计划，替代原交接文档中按月份划分的粗粒度时间线。计划以"研究周"为单位，不绑定自然周；若因其他工作中断，顺延研究周编号，不压缩验证环节。

每个研究周固定包含两类工作：

1. **研究日 A**：代码、实验配置、仿真或技术推导
2. **研究日 B**：复核、数据分析、形成图表并准备周会材料

每周必须产生可检查的证据。代码存在、配置写完或仿真启动都不单独视为完成。

### 2026-08-14 修订说明

本次修订基于两轮代码与实验配置的实地核对，改动如下（第一轮 4 项，第二轮 7 项）：

| 修订 | 原因 |
|---|---|
| 明确回环 FDL 与输入 FDL 是两个器件 | 原计划默认实验 C 可以用现有回环 FDL 验证 `offset→0`，但回环 FDL 要求 offset > 0 才能工作，论证链是断的 |
| 新增第 5 周"实验平台重建" | 原配置负载约 0.24%、W=1 被破坏、仿真确定性，实验 A/B 会跑出空结果 |
| 新增文献定位周和 Task 1.3 | 原 12 周内没有文献工作，且实验 B 依赖尚未重跑的第一阶段干净基线 |
| 新增论文并行线 | 项目 2027-09 结题，等第 12 周再动笔来不及审稿 |
| 修正 FDL 器件口径为光开关式可调延迟器 | 实际器件由光开关组成、非固定长光纤；物理约束改为最大延迟档、步进粒度、每级插损、切换时间 |
| 标注 BCP 处理时延为假设值并加敏感性扫描 | 1 µs 无器件手册依据、只对实验 C 敏感，实验 C 前须做 0.5/1/5 µs 扫描 |
| RateCheck 写成 go/no-go 闸门并预设判据 | 判据须在跑之前定死；主判据锚定稳定量，丢包率加绝对差兜底 |
| 第 6 周文献定位升级为 go/no-go 决策门 | 无差异化 novelty 时须给出替代发表口径或收缩建议 |
| 第 13 周输入 FDL 原型降级为可选/后备 | 第 12-14 周任务过重；时序模型必交付，原型可延至第 17 周 |
| 实验 C 新增迟到判据崩溃修复前置任务 | scheduleAt 调度到过去时刻会崩溃，offset 压向 0 时必然触发 |
| 新增正式决策请求表 D1/D2/D3 | 三项外部依赖需明确谁答、几号前、默认值，否则阻塞后期周次 |
| **2026-09-14：第 7–16 周按论文骨架重排** | 原计划把三周压在实验 C 上，按 2027-03 投稿倒推来不及；缺 VF、重传基线、ρ=0.8 热点三项。实验 C 进第 17 周起条件阶段 |

---

## 二、研究目标与技术口径

### 最终目标

实现卫星星间链路中的严格全光交换：数据 burst 在核心节点保持光信号，并进一步研究控制信息能否避免逐节点 OEO。

### 当前技术判断

1. FDL 是解决光域暂存和输出竞争的基础模块，但 FDL 实现不等于严格全光交换已经实现。
2. 当前独立 BCP 仍在核心节点进行电子处理，控制链路尚未实现全光闭合。
3. 第一篇研究重点是：在 W=1 与载荷可实现延迟器约束下，单级回环 FDL 相对无缓存+重传是否值得加。τ 扫描是证据，不是卖点。
4. 第一阶段边缘调度是受保护的实验基线，其研究价值通过实验 B「是否移动交叉点」体现；光标签等后续功能确有需要时允许受控扩展。实验 C 不进第一篇。

### 关键修正：回环 FDL 与输入 FDL 是两个器件

这是本次修订最重要的一条，它决定实验 C 和阶段四的设计。

现有实现中，burst 要进入 FDL，必须由控制逻辑在 `burstArrival - guardTime/2` 时刻预约 OXC，把入口门连到 FDL 出口门（`OBS_CoreControlLogic.cc:292-305`）。**这个预约本身要求 BCP 提前到达，即 offset > 0。** 当 offset = 0 时 OXC 尚未配置，burst 到达会因为 `schedulingTable[inGate] == -1` 被直接丢弃，回环 FDL 无法介入。

因此必须区分：

| | 回环 FDL（已实现） | 输入 FDL（阶段四需要） |
|---|---|---|
| 位置 | OXC 之后的回环端口 | 每个输入端口上，OXC 之前 |
| 数量 | 每节点 1 条 | 每节点 N 条 |
| 触发 | 有条件（发生冲突时） | 无条件（所有 burst 都经过） |
| 作用 | 解决输出通道竞争 | 补偿标签读取与 OXC 配置时间 |
| 对应问题 | 研究问题 ① ② | 研究问题 ③ |

**结论**：第二层的回环 FDL 工作有效且必要，但它回答的是"竞争缓解"，不能直接证明 `offset→0` 可行。实验 C 必须先建立输入 FDL 的时序模型，再谈可行边界。

### 关键修正：目标口径从 `offset=0` 改为"边缘 offset=0"

严格的 `offset = 0` 在物理上不可达——读标签和配置 OXC 必然消耗时间。可确认的目标口径是：

> **边缘节点发出时 offset = 0**（burst 与控制信息同时出发，不再提前发送独立 BCP），核心节点内部所需的处理时间由**输入端固定延迟线**吸收。

这个口径的好处是 FDL 从"可选的竞争缓解手段"变成"架构的必要组成部分"，且可以在仿真中被证伪。原 `research_status.md` 第七节挂起的两个待确认问题据此可以关闭。

### 可证明性边界

OMNeT++ 只能建模时序，不能证明光标签读取器件的存在性。论文可证明的上限是"数据面严格全光 + 控制面的定量时序预算"；控制面全光作为 future work。**不建议把论文标题绑定在严格全光控制面上。**

### 红线约束

- 保护 `src/EdgeNode/` 的第一阶段基线。与任务无关时不修改；功能确有需要时可扩展，但必须保留原模式语义、默认行为和旧实验可复现性。
- `useFDL=false` 必须与 FDL 开发前保持向后兼容。
- 所有结果必须来自 OMNeT++ 实际仿真。
- W=1，FDL 最多一次回环。
- τ 必须换算为具有卫星载荷可实现性的延迟器参数（最大延迟档、步进粒度、每级插入损耗、开关切换时间）。实际器件是由光开关组成的可调光纤延迟器，不是固定长光纤。
- 不引入缺少实验依据的复杂在线策略。

---

## 三、当前基线

| 工作项 | 状态 | 下一验收点 |
|---|---|---|
| 第一阶段边缘调度 | 已完成，作为受保护基线 | Task 1.3 干净重跑后才可用于实验 B |
| Task 2.1 FDL 模块与 CoreNode 集成 | 已提交 | 与调度逻辑一起完成回归验证 |
| Task 2.2 FDL 调度逻辑 | 已实现，静态复核通过 | release 编译 + 三场景时序 + 兼容性 |
| Task 2.3 FDL 功能测试 | 三分支已通过；当前树 ICMP 回归已通过（2026-08-20）；pre-FDL 对照有条件通过（2026-09-04，仅 `my ID` 不同） | 第 5 周起做负载标定 |
| 实验平台（W=1 / 负载 / 随机化） | 进行中（2026-09-04）：W=1 全量冻结、14 run、脚本可检查平台条件；k 未测得 | 用户编译后跑通 `FDL-Calibrate` 并回写 k |
| 实验 A/B + ExpR | 未开始 | 在功能基线冻结、M1 通过、热点负载可达到 ρ≥0.8 后依次推进 |
| 实验 C / 光标签原型 | 第二篇；第 17 周起 | 第 16 周交立项页；不进第一篇主线 |

---

## 四、复杂度判断

| 复杂度 | 判断标准 | 本项目任务 |
|---|---|---|
| 低 | 输入和输出明确，主要是整理与决策记录 | 方向确认、阶段总结、汇报材料 |
| 中 | 需要配置实验、批量运行和数据分析，但模块边界清晰 | Task 2.3、实验平台标定、实验 A、实验 B |
| 高 | 涉及跨模块时序、基线回归或研究假设可能被推翻 | Task 2.2 验证、实验 C、输入 FDL 原型 |

高复杂度任务也只安排一个研究周的明确子目标；若当周未通过验收，只延续该子目标，不直接进入下一阶段。

---

## 五、16 个研究周主计划

| 研究周 | 研究日 A | 研究日 B | 周会证据 | 复杂度 | 周验收门槛 |
|---|---|---|---|---|---|
| 第 1 周 ✅ | 形成严格全光交换定义、现状差距和两条候选路线 | 核对 Task 2.2 实现状态，建立可复现实验基线 | 架构差距图；待决策表 | 中 | 已完成（2026-08-13） |
| 第 2 周 | 修复 release 链接（`-linet`），完成 `make MODE=release` | 静态复核两段 OXC 预约、horizon 和 BCP 时间更新 | 编译结果；FDL 调度时序图；问题清单 | 高 | release 可执行文件生成，核心时序无已知阻断问题 |
| 第 3 周 | 运行 `fdl_tests.ini` 的三个确定性场景 | 核对 EV 日志、`.sca` 和 burst 到达时刻 | 三场景事件时间表；`fdlUsageCount` 证据 | 高 | 直通、回环、丢弃三分支均被触发且结果符合预期 |
| 第 4 周 | 运行 `Test-FDL-Compatibility` 与 pre-FDL 提交对照 | 修复回归差异，核对所有新增统计量定义 | 新旧 `.sca` 对照表；统计量检查表 | 高 | 已完成：当前树 ICMP 回归（2026-08-20）；pre-FDL 对照有条件通过（2026-09-04，仅 `my ID` 不同） |
| **第 5 周** | **运行 `FDL-Calibrate`，回写负载常数 k** | **核对 W=1 生效、多源流量成立、随机化有效** | **负载标定曲线；W=1 核对表；k 值** | **中** | **进行中（2026-09-04）：配置已就绪；验收仍为测得的 `maxChannelUtilization` 与目标 ρ 对齐，且不同 seed 确有差异** |
| **第 6 周** | **文献定位（go/no-go 决策门）：FDL/OBS 竞争缓解已有工作梳理** | **确定新颖性缺口与投稿口径；若无差异化 novelty 须给出替代发表口径或收缩建议** | **文献对照表；novelty 陈述一页** | **中** | **能明确回答"本文新在哪"；no-go 时须给出替代方案才算完成** |
| **第 7 周** | **Task 1.3：第一阶段实验干净重跑** | **用户跑 `Test-FDL-Basic/TooShort/Off` 过 M1；补 FDL 排队论检索** | **四模式对照图；M1 标量；排队论缺口表** | **中** | **实验 B 基线可信；M1 过；排队论读后登记 refs.bib** |
| 第 8 周 | 热点流量矩阵（`FDL-Hotspot`），确认瓶颈 ρ≥0.8 | 复标定；确认 D1 已发出（冻结日 2026-09-28） | 热点利用率表；种子差异 | 中 | 瓶颈链路实测 ρ≥0.8 且种子有差异 |
| 第 9 周 | 实现 VF 调度器选项（默认关） | 实现 host 侧重传基线（默认关）；两者的确定性用例 | 新用例 `.sca`；`useFDL=false` 回归 | 高 | **已完成（2026-09-20）**：`useFDL=false` 回归逐标量全同（1701/1701、1697/1697）；新用例 6 个全部按预期通过；另得负面结论（同步偏移下 VF 补 0 个 burst，附证明） |
| 第 10 周 | ExpA pilot：负载 × 调度器 × τ | 冻结网格与重复次数；载荷延迟器不等式写成文档 | 参数表；pilot 曲线 | 中 | **已完成（2026-09-22）**：参数表冻结（`research_reports/2026-09-week10/网格冻结.md`）；异步臂 54 run exit 0；pilot 曲线 `research_reports/figures/pilot_grid_freeze.png`。pilot 结论：τ 最优点随偏移模型移动（同步 τ/T=1、异步 Horizon τ/T=2）；异步下 VF 收益 7.4–8.9 pp ≥ FDL 收益 4.9–6.4 pp；偏移模型自身效应约 8 pp；热点 ρ≈0.81 上 FDL 只买到 0.1–0.6 pp |
| 第 11 周 | 批量：`ExpA-AsyncTauSweep`/`ExpA-AsyncNoFDL`/`ExpA-HotspotPoint` + 用**同一构建模式**重跑 `ExpA-TauSweep`/`ExpA-NoFDL` | 批量 `ExpR-Retransmit`（2 s） | 图 3–5 初版 | 中 | **已完成（2026-09-22）**：225 run（repeat 5，release）全部 exit 0；图 3–5 + `batch_summary.csv`；第 8 周判定为 debug 且 release 重跑逐位复现关键值；Experiment R 修掉确认包填充假象、查清开环可用边界（新发 ρ≈0.2）|
| 第 12 周 | 可行性图（`fdl_design.py` 接器件表，图 6 三区） | 运行 `FDL-RateCheck` | 图 6；归一化表 | 中 | **已完成（2026-09-22）**：图 6 双面板（有用区 τ/T∈[0.5,2]；电光 10.64 dB vs SOA 0.14 dB；MEMS/光束转向被不等式一定量排除）；**D2 关闭 = GO**（四项判据 PASS；两个计数量的 23% 原始偏差已由窗口比 1.3000 解释、按 T_burst 归一后 ≤0.06%）。**遗留可选**：给核心逻辑补两个拒绝原因计数（wait>τ / FDL busy）以充实图 4；ExpR 若进高负载须先做重传窗口 |
| 第 13 周 | 批量运行 `ExpB-Joint` | 分析：联合是否移动交叉点 | 图 7；burst 长度公平性表 | 中 | 只回答交叉点是否移动，不把边缘当主贡献 |
| 第 14 周 | 敏感性（突发长度、热点） | 论文第 3–5 章初稿 | 图 8；初稿 | 中 | 敏感性不推翻 5.2/5.3 |
| 第 15 周 | 全文初稿 | 内部评审 | 完整稿 | 中 | 结论与 `.sca` 一一对应 |
| 第 16 周 | 修改并投稿（目标 ≤ 2027-03） | 结题报告框架；实验 C 立项页 | 投出记录；C 立项 | 中 | 第一篇投出；实验 C 不混进本稿 |

### 论文并行线

从第 11 周起，每完成一组实验立即写入论文对应小节，不等到第 15 周。第 15–16 周只做整合与前后文，不做首次写作。骨架见 `research_reports/2026-09-week6-novelty/论文骨架.md`。研究叙事不在本文件复述，见 `research_status.md` 第十节。

---

## 六、阶段任务与验收标准

### Task 2.2：FDL 调度逻辑验证

| 验证点 | 通过标准 |
|---|---|
| 编译 | `make MODE=release` 成功 |
| 直通 | 目标通道空闲时只进行一次 OXC 预约 |
| 回环 | 冲突满足条件时进行两次 OXC 预约，并在 τ 后到达目标输出 |
| 丢弃 | FDL 不可用或等待条件不满足时正确记录竞争丢包 |
| horizon | FDL 与目标输出的预约区间不重叠、不提前释放 |
| BCP 时序 | 回环后到达偏移与 burst 的实际延迟保持一致 |
| 统计 | FDL/丢包/信道利用率指标可从 `.sca` 提取且口径无重复 |

**已澄清的一处语义**：FDL horizon 更新为 `burstArrival + burstDuration + 3g/4`，未加 τ。这是正确的——FDL 是延迟线不是缓存，多个 burst 只要入口时间不重叠就可以同时在光纤中传播，定长延迟保证出口顺序与间隔不变。horizon 建模的是 FDL **入口占用**。`fdlUtilization` 同理度量入口占用率而非光纤占空比，论文中必须写明这一定义。

### Task 2.3：功能测试基线

配置位于 `Examples/RingFdlOBS/fdl_tests.ini`。测试拓扑为 sat1 与 sat5 同时发往 sat3，两条路径在 sat2 汇聚于同一输出端口，传播时延均为 5 ms，sat5 延迟 5 µs 启动以产生确定性冲突。

| 测试 | 目的 | 关键判据 |
|---|---|---|
| Test-FDL-Basic | 证明回环分支可达（τ=20µs > waitTime） | `fdlUsageCount > 0`，两次 OXC 预约间隔恰为 τ，BCP offset 增加恰为 τ |
| Test-FDL-TooShort | 证明丢弃分支仍可达（τ=2µs < waitTime） | `fdlUsageCount == 0`，`burstLossContention > 0` |
| Test-FDL-Off | 证明开关关闭有效 | 所有标量与 Test-FDL-TooShort 一致 |
| Test-FDL-Compatibility | 证明原基线未被改变 | 与 pre-FDL 提交的 `.sca` 逐标量一致，新增标量除外 |

Test-FDL-TooShort 是本次新增的：缺了它，Test-FDL-Basic 通过也可能是"FDL 无条件接收一切"造成的。

### 实验平台（第 5 周）

配置位于 `fdl_params.ini` 的 `[Config FDL-Scenario]`，纠正了三个会使实验失效的问题：

| 问题 | 原状 | 修正 |
|---|---|---|
| W=1 被破坏 | `lambdasCore1to2 = 3`，即全网最拥塞的链路是唯一违反 W=1 的链路 | 恢复为 1，并同步修正 sat1/sat2 的 `lambdasPerInPort/OutPort` |
| 负载约 0.24% | 12 条流 × 500B/2ms = 24 Mbps 对 10 Gbps 信道 | 全互联流量 + 以目标 ρ 反解发送间隔 |
| 仿真确定性 | 固定 `sendInterval` 与固定 `startTime`，换种子结果相同 | 发送过程指数分布，启动时刻抖动 |

标定流程：运行 `[Config FDL-Calibrate]`（FDL 关闭，扫描发送间隔），用 `tools/calibrate_load.py` 读取实测 `maxChannelUtilization`，拟合 `ρ = k / meanInterval`，回写 k。拓扑、路由、burst 大小或信道速率任一改变都必须重新标定。

### 实验 A：τ 参数扫描

- 主问题：W=1 下，单级回环 FDL 在哪些 τ/T_burst 与负载上有效？这是第一篇 5.1 节的证据，**不是单独卖点**。
- 自变量：**τ/T_burst 比值**（0.25 / 0.5 / 1 / 2 / 4），以及负载、调度器（Horizon / VF）两个维度。
  **2026-09-20（第 9 周）修正**：调度器维度在现有"各源同 `maxOffset`"的同步偏移下**恒等于零**——实测 `voidFilledBursts = 0`，与 Horizon 逐标量相同（原因：到达顺序 ≡ BCP 顺序 ⇒ 被 horizon 拒绝者必与已有预约窗口重叠，无可补空隙）。故 VF 不能直接作为网格维度：需先裁决 D4（加"偏移异质"维度使 VF 有可比对象，或把 VF 降级为结构结论），再冻结网格。**解析文献已给出"最优粒度依赖 burst 长度分布与负载"（`laevens2003single`、`vanhoudt2004channel`，第 9 周已读全文），因此 τ/T_burst≈1 只能作为本文器件模型下的标定，不得写成新发现。**
- 计划负载 ρ∈{0.4, 0.6, 0.8}。全互联目前最重点约 0.59；ρ=0.8 必须用 `FDL-Hotspot` 实测后再写入网格，不得把 21.3 µs 标成 ρ=0.8。
- 指标：Burst Loss Rate、均值/95% 时延、FDL 利用率、拒绝原因分解、有效吞吐量。
- 工程输出：τ 与等效光纤长度/延迟器参数的换算区间（`tools/fdl_design.py`）。实际器件是由光开关组成的可调光纤延迟器，其物理约束由最大延迟档、步进粒度、每级插入损耗和开关切换时间决定，而非固定长光纤的 1 km 预算。延迟器档位范围、损耗预算、体积重量需向结构/总体专业确认；D1 冻结后用于第 12 周可行性图。

仿真在 1 Gbps 下进行，物理设计点为 10 Gbps。FDL 动力学只依赖无量纲比值（ρ、τ/T_burst、offset/T_burst），`[Config FDL-RateCheck]` 用于验证该归一化成立。

**RateCheck go/no-go 闸门**（go/no-go 即预设通过/不通过判据的决策闸门，通过才继续投入；判据必须在跑之前定死）：

主判据（稳定量，不受低丢包率放大效应影响）：

- `maxChannelUtilization` 相对偏差 < 10%；
- `fdlUsageCount` 相对偏差 < 15%；
- `carriedBursts` 相对偏差 < 10%。

辅助判据（丢包率，加绝对差兜底——低丢包率下相对偏差天然放大，如 0.1% vs 0.12% 的相对偏差 20% 会触发假 no-go）：

- `burstLossRate` 相对偏差 < 10% **或** 绝对差 < 0.1 个百分点。

go：主判据全部满足 → 按 τ/T_burst 归一化报告。no-go：改为在物理速率下重跑（需开发 burst 级流量源），或降级结论为"仿真速率下成立"。

**2026-09-22（第 12 周）执行结果：GO。** 需要一个 1 Gbps 的**孪生点**才谈得上比较，故新增 `[Config FDL-RateCheckRef]`（τ=34.4 µs、sendInterval=42.6 µs、2 s；与 10 Gbps 的 `FDL-RateCheck` 五项无量纲比值一一对应）。四项判据全部 PASS：`maxChannelUtilization` 0.21%、`fdlUsageCount` 0.06%、`carriedBursts` 0.00%、`burstLossRate` 0.10%（绝对差 0.0053 pp）。**两点必须记住**：(1) 两个计数量按**原始总数**比会差 23.03% / 23.07%，原因不是归一化失败而是**测量窗口**——两运行时长差 10 倍（时长随速率缩放）而 warmup 必须留在真实秒（排除每跳 5 ms 的填路瞬态），窗口为 43,655 vs 56,752 T_burst，比值 **1.3000** 与 23% 完全吻合，按 T_burst 归一后 ≤0.06%；判据在计数类量上只有归一化后才有内容，`tools/compare_ratecheck.py` 同时打印两列。(2) 本判定只覆盖 τ/T=1、sendInterval/T=1.24 一个点，不能外推到未测工作点；ρ 不可跨 0.2 s 与 2 s 的配置比较。表：`research_reports/2026-09-week12/ratecheck-output.txt`。

风险根源：`guardTime`、OXC 切换时间、BCP 处理时延是绝对时间，不随线速率缩放，这正是要验证的东西。

按 4295 B 的 burst 换算到 10 Gbps 设计点：

| τ/T_burst | 物理 τ | 等效光纤长度 | 光纤传输损耗 |
|---|---|---|---|---|
| 0.25 | 0.86 µs | 175 m | 0.04 dB |
| 0.5 | 1.72 µs | 351 m | 0.07 dB |
| 1.0 | 3.44 µs | 701 m | 0.14 dB |
| 2.0 | 6.87 µs | 1402 m | 0.28 dB |
| 4.0 | 13.74 µs | 2805 m | 0.56 dB |

注意：上表的"等效光纤长度"和"光纤传输损耗"是换算参考值。实际器件为**光开关式可调延迟器**，总损耗 = 光纤段损耗 + 级数 × 每级插入损耗（后者通常是主导项）。可行区间上界取决于延迟器的最大延迟档和损耗预算，需向结构/总体专业确认。降低线速率会拉长 burst 时长，从而要求更大的延迟和更多的切换级数。

- 结论边界：实验只证明当前模型和参数范围内的工作区间。

### 实验 B：边缘与核心联合对比

| 配置 | 边缘调度 | FDL |
|---|---|---|
| Baseline | Static | OFF |
| Edge Only | Dynamic | OFF |
| FDL Only | Static | ON |
| Combined | Dynamic | ON |

- 主问题：边缘调度是否**移动**回环 vs 重传的交叉点？（第一篇 5.4，一节，不是主线）
- 必须同时报告负载水平、重复次数、随机种子、离散程度和效应大小。
- **公平性要求（新增）**：调度模式会改变哪些包被组进同一 burst，因而改变 burst 长度分布，而 burst 长度通过 `waitTime` 直接进入 FDL 判据。四组配置因此不是干净的单变量对比。必须用每端口的 `carriedBytes[] / carriedBursts[]` 报告各组平均 burst 长度，确认差异可控后才能下"联合收益"的结论。
- 若收益不稳定，应如实调整论文叙事，不把"必要性"当作预设结论。
- 前置依赖：Task 1.3 完成，Static/Dynamic 基线数据干净。

### 实验 R：无缓存重传基线（第一篇 5.2）

- 主问题：回环 FDL 相对「冲突即丢 + 源端重传」的 goodput / 时延 / 丢包交叉点在哪里。
- 配置：`ExpR-Retransmit`。**2026-09-20（第 9 周）：host 侧模块已实现**（`src/Retransmit/`：`OBS_RetransmitSource` / `OBS_RetransmitSink` / `OBS_RetransmitPacket.msg`，`like IUDPApp`），配置已接线，不再是 No-FDL 占位。确定性用例 `Test-Retransmit-On/Off` 通过：完成时延差 30.00 ms 恰等于 `retransmitTimeout`。
- **2026-09-22（第 11 周）批量结果与两条硬约束**：
  1. **确认包必须按真实尺寸计费**。`ExpR-Retransmit` 内覆盖 `minSizeWithPadding = 64B`：场景默认 500 B 会把 64 B 确认填成 4 µs 的交换机占用（应 0.8 µs，×2.4），未修时该臂**不收敛**（活动消息 1.5 s 内 47k→729k，32 位进程 T=1.49 s 以 0xC0000005 静默退出）。
  2. **开环重传的可用区间只到新发 ρ≈0.2**。79.8 µs 稳定（活动消息 31k 平坦），35.6 µs（新发 ρ≈0.40）无界增长，60/120 ms 两个超时值皆然——丢包引发的重传 ≈ +30% 流量加确认流量把瓶颈推过容量，而栈内没有拥塞控制。要进高负载区必须先做**重传窗口（限速）**，属独立设计变更。
- 与回环臂的对照必须画在**实测负载轴**上：同实测负载（ρ≈0.42–0.43）下重传臂送达载荷 **1145 / 1126 Mbit/s**，回环臂 **2046–2520 Mbit/s**；重传臂 `retx/fresh ≈ 1.0`、线上有用率约 61%。两臂新发负载不同，不得用新发负载轴比较；也不得把重传臂的"请求送达比例"与回环臂的"包送达比例"直接相减。
- 超时是这条臂定义的一部分：60 ms 对最坏路径（5 跳 × 5 ms 每程）偏紧（实测平均完成时延 69 ms > 超时，266,536 次重传对 244,659 请求），故并列保留 120 ms 版本；两者送达比例相近（80.2% / 81.6%）。
- 不改 `src/EdgeNode/`。

### 实验 C：Offset 缩减

> **2026-09-14：** 移出 16 周主线，进第 17 周起条件阶段。下面技术约束仍有效，只是不再占用第一篇日程。

- 主问题：在输入 FDL 支撑下，边缘 offset 能推进到什么范围？
- 必须先建立输入 FDL 时序模型：`offset_edge = 0` 时，核心节点需要 `t_label + t_OXC` 的处理窗口，由输入 FDL 提供固定延迟 `τ_in ≥ t_label + t_OXC`。
- 必须区分 `offset > 0`、`offset≈0` 和严格 `offset=0`，并明确后者物理上不可达。
- **前置检查**：`params.ini` 中 `BCPProcessingDelay = 0s`、`OEConversionDelay = 0s`、`EOConversionDelay = 0s`。在这些值下 offset 沿路径永不消耗，实验 C 会得到"offset 任意小都可行"的虚假结论。`fdl_params.ini` 已将 `BCPProcessingDelay` 设为 **1 µs（假设值，assumed, to be confirmed against device datasheet）**。实验 C 正式运行前须对 `processingTime` 做敏感性扫描（0.5 / 1 / 5 µs），避免结论依赖单一未验证常数。该值对实验 A/B 几乎无影响，只对实验 C 敏感（每经过一个核心节点 offset 被扣掉一个 `processingTime`）。
- **前置任务：迟到判据崩溃修复**。当前代码 `OBS_CoreControlLogic.cc:150` 的 `burstArrival < simTime()` 为严格小于，`arrivalDelta == 0` 时放行进入正常流程，但第 255 行 `OXCConnectTime = burstArrival - guardTime/2` 会调度到过去时刻，导致 OMNeT++ 运行时崩溃。实验 C 将 offset 往 0 压的扫描中（当扫描点恰为 `processingTime` 整数倍，经足够多跳后剩余 offset 归零），以及第四阶段"边缘 offset=0"场景（第一跳核心节点 `arrivalDelta` 就是 0），都会触发此崩溃。启动实验 C 前须：（1）用 `offset=0` 做一次 smoke 运行实测复现崩溃；（2）将迟到判据改为 `burstArrival < simTime() + guardTime/2` 或在 `scheduleAt` 前加保护。
- 输出应是可行边界和失效原因，而不仅是丢包率曲线。

---

## 七、条件阶段：实验 C 与光标签最小原型

第一篇投稿后启动。先做实验 C（输入 FDL 时序），再决定是否进入原光标签最小原型。

| 研究周 | 任务 | 主要证据 | 复杂度 |
|---|---|---|---|
| 第 17 周 | 实验 C 立项；输入 FDL 时序模型 | 时序约束图；架构对比表 | 高 |
| 第 18 周 | 迟到判据修复；offset 缩减 pilot | smoke 日志；pilot 边界 | 高 |
| 第 19 周 | 实验 C 批量（有/无输入 FDL） | offset 边界曲线 | 高 |
| 第 20 周 | 是否启动光标签最小原型的决策 | 决策页；若 Go 则沿用原消息模型任务 | 高 |

若实验 C 不能支持光标签路线，第 20 周改为研究替代架构，例如保留极小正 offset 的过渡方案，不机械进入 `offset=0` 实现。

---

## 八、每周汇报规范

每周汇报控制在一页核心内容，最多包括：

1. **本周问题**：本周要回答的唯一核心问题
2. **工作与证据**：一张主图或时序图，加一张小表
3. **研究判断**：证据支持什么、不支持什么
4. **风险与下周门槛**：下一周开始前必须满足的条件

建议图表类型：

| 工作类型 | 首选呈现方式 |
|---|---|
| 代码与功能验证 | 事件时序图 + 测试通过表 |
| 参数扫描 | 折线图/误差带 + 参数区间表 |
| 方案对比 | 分组柱状图或点区间图 + 公平性表 |
| 架构判断 | 证据链图 + 已证明/未证明对照表 |

---

## 九、文档协同规则

- 本文件记录稳定的研究计划、任务边界和验收标准。
- `research_status.md` 记录每周状态、实际证据、偏差和下一周动作。
- `walkthrough.md` 记录最近一次代码实现和验证细节。
- 每周会后更新 `research_status.md`；只有路线或任务边界发生变化时才修改本文件。

### 关键文件索引（第二阶段新增）

| 文件 | 用途 |
|---|---|
| `Examples/RingFdlOBS/fdl_params.ini` | FDL 场景基线：W=1、全互联随机流量、负载旋钮 |
| `Examples/RingFdlOBS/fdl_tests.ini` | Task 2.3 三分支确定性功能测试 |
| `Examples/RingFdlOBS/fdl_experiments.ini` | 负载标定、实验 A、实验 B、ExpR 占位、热点流量、归一化校验 |
| `Examples/RingFdlOBS/tools/calibrate_load.py` | 由实测信道利用率反解发送间隔 |
| `Examples/RingFdlOBS/tools/fdl_design.py` | τ ↔ 光纤长度/延迟器参数 ↔ 损耗预算换算 |

---

## 十、正式决策请求表

| 编号 | 谁能回答 | 具体问什么 | 阻塞哪个研究周 | 回答截止时间 | 无回答时的默认值 | 状态 |
|---|---|---|---|---|---|---|
| D1 | ~~结构/总体专业~~ **改为公开文献/厂商资料（2026-09-20 决定）** | 卫星载荷可调光延迟器参数：最大延迟档、步进粒度、每级插入损耗、开关切换时间、质量体积预算 | 第 10 周（参数冻结）与第 12 周（可行性图） | 已闭合（2026-09-20） | **器件部分已从 Mouammar 2026 TABLE II 及其原始出处建表**（Polatis / GLsun / Agiltron 厂商页 + Feyisa 2022 *JLT*）：切换时间与插损**有出处**；**MEMS 被定量排除**（τ ≥ 8 ms → τ/T_burst ≈ 2.3×10³，而实测最优在 τ/T_burst = 1）。**仅剩"质量/体积/功耗预算"开放** → 改为显式假设 + 敏感性，不阻塞第 12 周。**更正**：旧默认"每级插损 0.5 dB"过于乐观（Agiltron 实际 **3.5 dB/级**，τ/T=1 时总插损 10.64 dB vs InP SOA 的 0.14 dB）。文档：`research_reports/2026-09-week8/器件参数与三条不等式.md`；原询问稿已作废 | **已闭合（剩余一项用假设）** |
| D2 | 导师/评审 | 1 Gbps 仿真、τ/T_burst 归一化报告是否可接受？ | 第 12 周（RateCheck） | 第 10 周前 | 按 go/no-go 闸门自行判定 | **已关闭 = GO（2026-09-22）**：四项判据 PASS（0.21% / 0.06% / 0.00% / 0.10% 与 0.0053 pp）；仅覆盖 τ/T=1 一个点 |
| D3 | 器件手册（或用户默认值） | BCP 电子处理时延实际取值 | 第 18 周（实验 C） | 第 17 周前 | 1 µs（假设值） | **已关闭**——暂用 1 µs，实验 C 前做 0.5/1/5 µs 敏感性扫描 |
