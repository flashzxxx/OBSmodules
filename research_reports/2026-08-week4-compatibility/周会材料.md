# 第 4 周周会材料：ICMP 兼容性

> 日期：2026-08-20；2026-09-04 补 pre-FDL 提交对照  
> 本周核心问题：关掉 FDL 后，第一阶段 ICMP 场景的旧标量有没有被改掉？

## 一页结论

`Test-FDL-Compatibility` 已跑完。20 个 ping 全到；全网 FDL 计数为 0。

相对干净树 `3530c2f`（无 FDL 模块）的 `ICMPTest`：共有 1332 条标量，取值不同的 72 条全是 `my ID`（CoreNode 多了 `fdl` 后模块编号后移）。排除 `my ID` 后 1252 条全部相同；多出的 235 条全是后加的 FDL / 利用率 / 丢包统计。pre-FDL 没有缺标量。

相对 8 月 13 日已含 FDL 代码的 `ICMPTest`：1422 条共有标量完全一致，多出 145 条全是后来加的统计量。

> 当前结论：关 FDL 时旧 ICMP 路径稳定；计划原文的 pre-FDL 对照有条件通过（仅 `my ID` 不同）。

## 本周证据

| 检查 | 结果 |
|---|---|
| ping 20/20、loss 0（两边） | 通过 |
| 10 个节点 `fdlUsageCount`、器件计数、`burstLossContention` | Compatibility 全 0 |
| 相对 `results/pre-fdl-baseline/ICMPTest-0.sca`（`3530c2f`） | 1332 共有、72 差全是 `my ID`、235 新统计；排除 `my ID` 后 0 差 |
| 相对 `results/ICMPTest-0.sca`（8-13，已含 FDL 代码） | 1422 同、0 差、145 新统计 |

原始数据：`Examples/RingFdlOBS/results/Test-FDL-Compatibility-0.sca` 与 `results/pre-fdl-baseline/ICMPTest-0.sca`。明细见同目录 `分析.md`。

## 下周门槛（第 5 周）

运行 `FDL-Calibrate`，用 `tools/calibrate_load.py` 回写负载常数 k。pre-FDL 标量对照已补完，不再作为第 5 周前置缺口。
