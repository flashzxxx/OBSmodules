# 第 8 周：ExpA 批量（提前）+ 热点流量标定

> 日期：2026-09-18
> 分支：`task/code-review-2026-09`
> 同周材料：`周会材料.md`
> 相关：`research_reports/2026-09-week7/`（第 7 周：M1、guardTime 根因、Task 1.3、排队论）
> 输出：`Results/ExpA-TauSweep/`、`Results/ExpA-NoFDL/`、`Results/FDL-Hotspot/` 及三个标定目录

---

## 一、本周任务与验收

计划原文（`codex_phase2_tasks.md:136`）：

| 研究日 A | 研究日 B | 周会证据 | 验收门槛 |
|---|---|---|---|
| 热点流量矩阵（`FDL-Hotspot`），确认瓶颈 ρ≥0.8 | 复标定；确认 D1 已发出（冻结日 2026-09-28） | 热点利用率表；种子差异 | **瓶颈链路实测 ρ≥0.8 且种子有差异** |

用户另要求**提前执行第 11 周的 `ExpA-TauSweep` 批量**（原计划第 11 周）。

| 事项 | 结果 |
|---|---|
| ExpA 批量（提前） | ✅ 完成，且途中查出并修复一个红线缺陷后重跑 |
| 热点 ρ≥0.8 | ✅ **19.5 µs → ρ = 0.8074**，种子有差异 |
| 复标定 | 第 7 周已提前完成（ρ=0.40 → 35.6 µs） |
| D1 | 询问稿**未发出**；Agent 判断：建议发，但不阻塞第 8–11 周 |

---

## 二、ExpA-TauSweep 批量（提前执行第 11 周）

### 2.1 网格与产出

`ExpA-TauSweep`：`useFDL=true`，τ 按突发时长比值扫 5 档 × 2 档负载 × repeat 5 = **50 run**；
参考线 `ExpA-NoFDL`：同 2 档负载 × repeat 5 = **10 run**。全部 exit 0。

- 新增分析脚本：`tools/plot_expA_tausweep.py`
- 图：`results/ExpA-TauSweep/expA_tau_sweep.png`
- **未含**：第三档负载 ρ≈0.81（本周才标定出来）、VF 调度器维度（第 9 周实现）——留待第 10 周冻结网格后补跑

### 2.2 结果：τ 存在明确最优区间

| τ/T_burst | ρ≈0.40 丢包 | FDL 使用率 | ρ≈0.59 丢包 | FDL 使用率 |
|---|---|---|---|---|
| 0.25 | 10.79% | 3.3% | 16.32% | 4.8% |
| 0.50 | 9.09% | 6.4% | 14.76% | 9.0% |
| **1.00** | **6.78%** | 12.0% | **13.51%** | 15.3% |
| 2.00 | 8.35% | 15.4% | 16.38% | 19.4% |
| 3.99 | 11.74% | 20.9% | 19.61% | 24.2% |
| **No-FDL** | **12.88%** | — | **18.71%** | — |

三条可直接写进 5.1 的结论：

1. **最优在 τ/T_burst ≈ 1**，两档负载一致（ρ=0.40 时 −6.10 pp，ρ=0.59 时 −5.20 pp）。
2. **延迟线太长不但无益，还可能有害**：ρ=0.59、τ/T=4 时丢包 19.61% **高于**不加 FDL 的 18.71%。
3. **FDL 使用率随 τ 单调上升**（3.3% → 24.2%），但丢包先降后升 —— 也就是"用得更多 ≠ 效果更好"。
   这是 5.1 最有说服力的一张图：它把"收益边界"从直觉变成了数据。

---

## 三、途中查出的红线缺陷：`maxFdlLoopsPerBurst` 设成了无限制

### 3.1 现象

第一遍跑完，分析脚本的健全性检查报警：

```
total burstsLoopedMultiple across all runs = 147935 (must be 0)
```

`AGENTS.md` 红线写明"**单次回环**：FDL 最大回环次数为 1，不实现多次回环"，
但 `fdl_params.ini` 里是：

```ini
**.sat*.coreSwitch.maxFdlLoopsPerBurst = -1     # -1 = 无限制
```

代码逻辑是 `if (maxFdlLoopsPerBurst >= 0 && bcp->getFdlLoopCount() >= maxFdlLoopsPerBurst) fdlAllowed = false;`
—— 取 -1 时这个判断永不成立。而 **BCP 携带 `fdlLoopCount` 跨节点传递**，
所以一个突发可以在**沿途每个节点各回环一次**（最长 5 跳就是 5 次）。

### 3.2 配对探针量化

同种子、同单元，只改这个参数（ρ≈0.59）：

| τ | `loops=-1` | `loops=1` | 差 |
|---|---|---|---|
| 34.4 µs（τ/T=1） | 12.64% | **13.48%** | +0.84 pp |
| 137.4 µs（τ/T=4） | 23.30% | **19.61%** | **−3.69 pp** |
| 多次回环次数 | 21,818 / 41,997 | **0** | — |

结论：**这不是表面问题，最大改变 3.7 个百分点**；但定性结论活了下来
（τ/T=4 仍然差于 No-FDL 的 18.71%，只是差距从 4.6 pp 缩到 0.9 pp）。

### 3.3 处置

1. `fdl_params.ini` 改为 `**.sat*.coreSwitch.maxFdlLoopsPerBurst = 1`，并把探针数据写进注释。
2. **重跑整个 ExpA 批量**（50 run，1366 s）→ 第二节的表格即修正后结果，`burstsLoopedMultiple = 0` ✓
3. 第一遍（多回环版）数据保留在 `results/ExpA-TauSweep-multiloop-2026-09-18/` 作为对照证据。
4. 折进 `fdl_tests.ini` 两个常驻诊断配置：
   - `Test-FDL-Diag-LoopStrict` → `burstsLoopedMultiple` **必须 0**（实测 0 ✓）
   - `Test-FDL-Diag-LoopLoose` → **预期 >0**（实测 41,997 ✓，与探针一致）

---

## 四、热点流量标定：ρ≥0.8 达成

### 4.1 先搞清拓扑与瓶颈

去 sat2 的流量分三路（据各核心路由表）：

| 到 sat2 的入口 | 源 |
|---|---|
| 经 **sat3** | sat3, sat6, sat7, sat9, sat10（**5 个**） |
| 经 sat1 | sat1, sat4 |
| 经 sat5 | sat5, sat8 |

热点矩阵（9 源全发 sat2）本来就把 5 个源压在同一条链路上，所以 **瓶颈恒为 sat3 port1（sat3→sat2）**。

### 4.2 粗扫（40 run）+ 细扫（16 run）

| 发送间隔 | 瓶颈 ρ | 种子有差异 |
|---|---|---|
| 35.6 µs | 0.6245 | ✓ |
| 25 µs | 0.7253 | ✓ |
| 21.3 µs | 0.7764 | ✓ |
| 20.5 µs | 0.7888 | ✓ |
| 20 µs | 0.7967 | ✓ |
| **19.5 µs** | **0.8074** | ✓ |
| 19 µs | 0.8146 | ✓ |
| 18 µs | 0.8327 | ✓ |
| 15 µs | 0.8936 | ✓ |
| 12 µs | 0.9455 | ✗ **完全相同** |

**第三档负载点定为 19.5 µs → ρ ≈ 0.807**，已回写 `FDL-Hotspot` 的 `meanInterval` 扫描。

### 4.3 两个值得记的观察

1. **中途我一度以为 ρ 封顶在 0.78**：只扫到 21.3 µs 时看起来饱和了（丢包已 34.65%）。
   继续缩短间隔证明是误判 —— 18 µs 就上到 0.83，15 µs 到 0.89。
   教训：**"看起来饱和"要扫穿才敢下结论。**
2. **12 µs 时 4 个种子的 ρ 完全相同**（0.9455）且最忙节点在 sat1/sat4 之间跳变 ——
   链路被完全占满，ρ 钉在天花板上，与种子无关。这说明 12 µs 已超出可用工作区，
   不适合作为负载工作点。

---

## 五、D1 询问稿的判断

**建议发出**，理由：

| | 说明 |
|---|---|
| 收益 | 论文核心图（图 6 载荷可行性图）的立论就是"这个 τ 星上装得下"。参数若全为自拟，审稿人一句质疑即可击穿。 |
| 成本 | 一页纸、一句话的事。 |
| 风险 | 兜底默认值取自 Mouammar 2026 开关表。若结构专业回复"只能用 MEMS"，MEMS 切换 ≥8 ms 而突发仅 34.4 µs —— **回环窗口不成立，第一篇主结果消失**。这个二元结果值得先问。 |
| 量级 | 最长 τ 档换算到 10 Gbps 设计点约 **2.8 km 光纤**，装不装得下是结构专业一句话能答的。 |
| 不阻塞 | 第 8–11 周所有实验都不依赖 D1；只影响**第 12 周**的图 6。 |

**若决定不发**：建议不要"不管它"，而是在 9/28 明确写下"冻结默认值"这个决定，
否则第 12 周画图 6 时会变成"自己拍了个数字且没记录来源"。

---

## 六、未执行

- **第三档负载（ρ≈0.81）尚未并入 `ExpA-TauSweep`**：热点矩阵与全互联是两套流量，
  需要给 ExpA 加一个"流量形态"维度，属第 10 周冻结网格时要定的设计
- **VF 调度器维度未加**：`enableVoidFilling` 参数第 9 周才实现，配置里仍是注释
- 热点实验用的是 `numPackets=3`（与全互联一致），但**未复核突发长度在两种流量下是否可比**
- D1 未发出；排队论两篇全文（`laevens2003single`、`vanhoudt2004channel`）未取得
- release 构建未复跑；未提交 git
