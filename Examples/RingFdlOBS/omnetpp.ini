# ============================================================================
# OBS Satellite Network Simulation - Main Configuration
# 10-node ring FDL-OBS topology
# ============================================================================

# General parameters and network configuration
include params.ini

# Functional test configurations (ICMPTest, UDPTest, TCPTest, etc.)
include tests.ini

# Performance comparison experiments (FlowScaling, LoadIntensity, QueueSensitivity)
include experiments.ini

# Phase-2/3 FDL work. These only add [Config] sections and never touch
# [General], so the phase-1 configs above keep their exact behaviour.
include fdl_params.ini
include fdl_tests.ini
include fdl_experiments.ini
